databaseChangeLog:

  - changeSet:
      id: 005-create-historical-fuel-price-table
      author: jorge

      changes:

        - createTable:
            tableName: historical_fuel_price

            columns:

              - column:
                  name: id
                  type: uuid
                  constraints:
                    primaryKey: true
                    nullable: false

              - column:
                  name: station_id
                  type: uuid
                  constraints:
                    nullable: false

              - column:
                  name: station_product_type
                  type: varchar(50)
                  constraints:
                    nullable: false

              - column:
                  name: price
                  type: numeric(10,3)
                  constraints:
                    nullable: false

              - column:
                  name: observed_at
                  type: timestamptz
                  constraints:
                    nullable: false

        - addForeignKeyConstraint:
            baseTableName: historical_fuel_price
            baseColumnNames: station_id
            referencedTableName: station
            referencedColumnNames: id
            constraintName: fk_historical_price_station
            onDelete: CASCADE

        - addUniqueConstraint:
            tableName: historical_fuel_price
            columnNames: station_id, station_product_type, observed_at
            constraintName: uk_historical_fuel_price

        - createIndex:
            tableName: historical_fuel_price
            indexName: idx_historical_fuel_price_station_product_observed
            columns:
              - column:
                  name: station_id
              - column:
                  name: station_product_type
              - column:
                  name: observed_at